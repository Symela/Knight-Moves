package com.example.chess_knight_path;

class Node {
    // (x, y) represents chess board coordinates
    public int x, y;

    public Node(int x, int y) {
        this.x = x;
        this.y = y;
    }
};